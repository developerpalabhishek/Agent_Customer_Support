# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository (`Agent_Customer_Support`) is currently empty — no source files, dependencies, or commits exist yet. There is no build, lint, or test tooling to document.

Once the project is scaffolded (language/framework chosen, initial source added), re-run `/init` or update this file directly with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture notes describing how the major parts of the codebase fit together
